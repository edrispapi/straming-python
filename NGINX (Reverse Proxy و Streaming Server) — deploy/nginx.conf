# NGINX config for stream & reverse proxy
server {
    listen 80;
    server_name example.com;

    location /api/ {
        proxy_pass http://backend:8000/;
    }

    location /media/ {
        alias /app/media/;
    }

    location /live/ {
        proxy_pass http://streaming:1935/;
    }
}
